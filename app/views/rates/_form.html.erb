<fieldset class="box tabular">
  <legend><%= l(:label_new_rate) %></legend>
  <p>
    <%= f.label :project_id %>
    <%= project_select_tag(Rate.for(@user) ? (@rate.project || @project) : nil) %>
  </p>
  <p>
    <%= f.label :amount, value: l(:label_rate) %>
    <%= f.text_field :amount, size: 10 %>
  </p>
</fieldset>

<%= f.hidden_field :user_id %>
